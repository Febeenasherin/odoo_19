<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
<record  id="simple_production_line_view_list" model="ir.ui.view">
        <field name="name">components</field>
        <field name="model">simple.production.line</field>
        <field name="arch" type="xml">
            <list>
                        <field name="production_id"/>
                        <field name="product_id"/>
                        <field name="quantity"/>
<!--                        <field name="request_type"/>-->
<!--                        <field name="vendor_ids" invisible="request_type == 'internal transfer'"/>-->
<!--                        <field name="source_id" invisible="request_type == 'purchase order'"/>-->

<!--                        <field name="destination_id" invisible="request_type == 'purchase order'"/>-->
            </list>
        </field>
    </record>

    <record id="material_request_line_action"  model="ir.actions.act_window">
        <field name="name">simple production line</field>
        <field name="res_model">simple.production.line</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="view_content">
                no record found.
            </p>
        </field>
        </record>
</odoo>