<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--    list-->
    <record  id="migration_po_line_view_list" model="ir.ui.view">
        <field name="name">migration.product.line.list</field>
        <field name="model">purchase.migration.line</field>
        <field name="arch" type="xml">
            <list>
                        <field name="product_id"/>
                        <field name="product"/>
                        <field name="quantity"/>
                        <field name="unit_price"/>
                        <field name="amount"/>
<!--                        <field name="vendor_ids" invisible="request_type == 'internal transfer'"/>-->
<!--                        <field name="source_id" invisible="request_type == 'purchase order'"/>-->

<!--                        <field name="destination_id" invisible="request_type == 'purchase order'"/>-->
            </list>
        </field>
    </record>

    <record id="product_line_action"  model="ir.actions.act_window">
        <field name="name">product line</field>
        <field name="res_model">purchase.migration.line</field>
        <field name="view_mode">list,form</field>
<!--        <field name="help" type="html">-->
<!--            <p class="view_content">-->
<!--                no record found.-->
<!--            </p>-->
<!--        </field>-->
        </record>
</odoo>
